<h2><%= @venue.name %></h2>

<% if @venue.venue_image %>
<%= image_tag(@venue.venue_image) %><br>
<% end %>
<%= @venue.description %><br>
Location:<br>
<%= @venue.location %><br>
Times:<br>
<% @timeslots.each do |timeslot| %>
  <%= timeslot.start_time.strftime("%A %l:%M %p") %> - <%= timeslot.end_time.strftime("%l:%M %p")  %>, <%= timeslot.act.name %> <%= link_to 'Edit', edit_timeslot_path(timeslot) %> - 
<%= link_to 'Delete', timeslot_path(timeslot), method: :delete, data: {confirm: 'Are you sure you want to delete this time slot?'} %><br>
<% end %>

<br><br>

<%= link_to "Edit Stage Profile", edit_venue_path(@venue) %>- 
<%= link_to "Add Performance Times", new_timeslot_path %> 